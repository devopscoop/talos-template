# Arch Linux package list for talos-template — the pacman/AUR equivalent of
# the Brewfile. Installs every CLI tool used or referenced by this repo.
#
# All packages are in the official repos, so plain pacman works (as do yay/paru):
#
# Usage:
#   grep -vE '^(#|$)' pkglist.txt | sudo pacman -S --needed -

# talosctl — generates Talos secrets/config, applies config, bootstraps the
# cluster, fetches kubeconfig, upgrades nodes. The docs target Talos v1.11.x.
talosctl

# kubectl — merging kubeconfigs and using the cluster
kubectl

# Optional — only for the virt-manager (QEMU/KVM) path instead of ProxMox.
# Uncomment to install:
# virt-manager
# swtpm
# networkmanager
